# Server
PORT=4000

# Exposed via GET /api/mongo-defaults to prefill the Test Load panel's
# MongoDB URI/database fields, so a real connection string (e.g. an Atlas
# SRV URI with credentials) doesn't need to be retyped into the browser
# each session.
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=migrator_test
