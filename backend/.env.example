# Server
PORT=4000

# Used only for the "test load" endpoint (loading a sample batch into Mongo
# so you can sanity-check the mapping before running the full Glue job)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=migrator_test
